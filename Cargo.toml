[package]
name = "safe-inference"
version = "0.1.0"
edition = "2024"
description = "Safe ONNX inference with explicit TensorRT compilation and backend selection"

[dependencies]
anyhow = "1.0.104"
onnx-rs = "0.1.2"
ort = { version = "=2.0.0-rc.13", default-features = false, features = ["std", "load-dynamic", "tensorrt", "cuda", "api-27"] }
serde_json = "1.0.151"

#[features]
#openvino = ["ort/openvino"]
#directml = ["ort/directml"]
#coreml = ["ort/coreml"]

#[[bench]]
#name = "yolo11m_speed"
#harness = false
